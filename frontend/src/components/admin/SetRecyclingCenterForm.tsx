"use client";

// Formulario para establecer la dirección del contrato RecyclingCenter
// Debe recibir la dirección del contrato
// Al enviar, debe llamar a setRecyclingCenter del contrato CircularNFT usando wagmi/ethers

export function SetRecyclingCenterForm() {
  return (
    <form className="space-y-4 max-w-md">
      {/* TODO: Input para dirección del contrato RecyclingCenter */}
      {/* TODO: Botón para guardar dirección */}
      {/* TODO: Mostrar feedback de éxito/error */}
    </form>
  );
} 